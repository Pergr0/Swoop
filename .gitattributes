# Keep shell scripts and Makefiles LF so they run on Linux/macOS regardless of
# the OS they were edited on. PowerShell stays CRLF for Windows.
*.sh      text eol=lf
*.bash    text eol=lf
Makefile  text eol=lf
*.go      text eol=lf
*.ps1     text eol=crlf
